<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="ExpertDialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@drawable/bg_dialog_shap</item>
    </style>
    <style name="animation_prelogin">
        <item name="android:windowEnterAnimation">@anim/prelogin_in</item>
        <item name="android:windowExitAnimation">@anim/prelogin_out</item>
    </style>
</resources>
